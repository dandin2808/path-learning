# NN — <Stage name>

## The key idea

(1 short paragraph. What does this stage add to the lineage, in plain language? Compare to the previous stage if helpful.)

## Key equations (stated, not derived)

State the main equations or definitions. Use plain ASCII or LaTeX as appropriate for the format the card will be read in. Examples:

      f_theta(x; w)  =  <network output>
      L(w; D)        =  <loss>
      w_MAP          =  argmin_w  L(w; D) + lambda * ||w||^2

For each non-obvious symbol, give a one-line gloss. The full derivation lives in the linked paper.

## How it connects to neighbouring stages

- **Previous (NN-1):** what flowed in.
- **Next (NN+1):** what this enables.

## Caveats / failure modes

(1–2 paragraphs. Where does this approach break? What does it silently get wrong? What's the central limitation that motivates the next stage?)

## References

- [Anchor paper (Authors, Year)](https://arxiv.org/abs/...) — *what to read for the derivation*.
- [Companion / textbook chapter](https://...) — *what to read for context*.
- [Reference implementation](https://github.com/...) — *what to read for code*.
- (1–2 more if genuinely useful; avoid bloat.)
